# Ayotobi Oromiye Holo — PhD website

Research website. Content lives in `src/content/` (editable at `/admin` via Decap CMS).

```
npm install
npm run dev     # local preview
npm run build   # production build to dist/
```
